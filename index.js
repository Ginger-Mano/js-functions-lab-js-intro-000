function happyHolidays() {
returns (`Happy holidays!`)
}

function happyHolidaysTo(name) {
 var name = "Sal";
 returns (`Happy holidays, ${name}`);
}

function happyCustomHolidayTo(Easter, name) {
  var holiday = "Easter"
  var name = "Sal"
  returns (`Happy ${holiday}, ${Sal}`)
}

function holidayCountdown(Easter, 5) {
  var holiday = "Easter"
  var days = "5"
  returns (`It\'s ${days} days until ${holiday}!`) 
}
